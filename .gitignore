# Monorepo root. Each subproject has its own .gitignore:
#   frontend/.gitignore  (Next.js: node_modules, .next, env, etc.)
#   backend/.gitignore   (Python: .venv, __pycache__, *.db, .env)

.DS_Store
*.pem
