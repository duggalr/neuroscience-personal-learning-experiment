# Copy to .env and fill in. The backend loads .env automatically.
OPENAI_API_KEY=sk-...
# Tutor model (Responses API, supports web_search + streaming). Used from M1 on.
MODEL=gpt-5.4
# The student's name — used in the greeting and the tutor's system prompt.
USER_NAME=Rahul
